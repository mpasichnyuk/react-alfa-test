import React from "react";

const Loading = () => {
    return (
        <div className="loading">
            <h1>Загружаем информацию от турах...</h1>
        </div>
    );
};

export default Loading;
